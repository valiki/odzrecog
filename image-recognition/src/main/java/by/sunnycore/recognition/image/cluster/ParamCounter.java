package by.sunnycore.recognition.image.cluster;

/**
 * Base Interface for classes that are going to count some params of the pixel
 * 
 * @author Valiantsin Shukaila
 * 
 */
public interface ParamCounter {
	double[] count(double[] data);
}
